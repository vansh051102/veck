# Database

PostgreSQL (Supabase) via Prisma. See [`prisma/schema.prisma`](../prisma/schema.prisma) — the single source of truth for all models, fields, and relations. Migration commands and connection setup live in [`SETUP.md`](SETUP.md).
